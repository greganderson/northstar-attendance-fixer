# Northstar Attendance Fixer

## How to run

Setup virtual environment:

```bash
python -m venv .venv
```

Install requirements:

```bash
source .venv/bin/activate
pip install -r requirements.txt
```

Run:

```bash
python update_northstar_attendance.py <attendance-xlsx-file>
```
